[project]
name = "video-ai-detector"
version = "2.0.0"
description = "Estimate how likely a video is AI-generated by fusing visual + audio deepfake detectors."
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
filterwarnings = ["ignore::DeprecationWarning"]
